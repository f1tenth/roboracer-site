/**
 * The landing's "Start here" (ui/StartHere) sits under the hero at #start; the
 * nav's "Start here" button links there from every route, /about's "Start
 * here on the home page" too. One constant so the id and the links cannot
 * drift apart.
 */
export const START_ID = "start";
export const START_HREF = `/#${START_ID}`;
