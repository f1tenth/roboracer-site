# p3-fixes-2: code-review findings (docs/reviews/p3-code-review.md)

Branch `revamp/p3-fixes-2` off `revamp/polish-2`, dev server :4192, Chromium via Playwright.
Scripts: `docs/qa/p3-fixes-2/verify_hash.py` (M1, M2, L2), `docs/qa/p3-fixes-2/verify_rows.py` (L4).
All DOM measurements, no screenshots. Lint and build pass; zero console errors on the routes checked.

- **M1 done** (165726b): `topOf` subtracts the target's computed `scroll-margin-top`. SectionHeader's h2 gets nav + 1rem, plus the eyebrow line and its mb-4 when an eyebrow is present (otherwise the "04" still sat under the bar). `/race#leaderboard` and the /about Start here section get nav + 1rem. Measured after landing, eyebrow top vs nav bottom: /about#about-spinoffs 81 vs 67 (1536x730) and 88 vs 72 (390x844); /about#about-start 81/67 and 88/72; /race#leaderboard 210/67 and 148/72; headings fully in the window in every case. The landing `/#start` gets no margin on purpose: its nav-tall top padding already clears the bar, and a margin would add a second nav's height of empty space. It lands exactly where it did before (section top at y = 0, eyebrow at 94 / 104 px), so it moved 0 px.
- **M2 done** (c61c95a, 90b2535): during the hold a ResizeObserver on body and html re-applies the jump whenever the document's height changes, alongside the ScrollTrigger refresh listener, and is disconnected on release. With every `/data/*.json` fetch held back and scroll anchoring off (`overflow-anchor: none`, to stand in for Safari), a 2.5 s delay landed right. A 6 s delay still landed among the partner logos, because the data arrived after the old 4 s hold had ended. The hold is now 10 s, still ended by any wheel, touch, key or pointer input, and both delays land right at 1536 and 390. WebKit itself was not tested: Playwright's WebKit is missing host libraries here.
- **L1 done** (a85a672): `before <= after`. For n = 2, 4 and 11 the ranks run 1 to n - 1 with no gap.
- **L2 done** (c61c95a): `useScrollToHash` returns early on a POP that is not the page's first arrival. On /, clicking "Start here", scrolling 1500 px, then Back and Forward returns to the scrolled spot at +100, +500 and +1500 ms (3836 of 3837 at 1536, 3189 of 3189 at 390), without gliding and without moving focus to #start. A direct load of a hash URL (also a POP) still jumps. `docs/qa/p3-scroll/repro_route_scroll.py`: the mouse profile is all OK, including the landing's and the rulebook's Back/Forward. The touch profile's route checks are OK, but the script then crashed at its own "nav click during a scroll glide" step: its DOM `click()` on the mobile-menu link found no element. That step changes route and does not touch hash code, so I read it as a harness issue; not investigated further.
- **L4 partly done** (ff33d8c): below `desktop:`, a `<picture>` source (the compact query) draws the card-size file the row thumb was cut from. It is named by a new `row_thumbnail_large` field on the 16 papers whose THUMBS.md row says "downscaled from" a file in the repo; the pairs were checked side by side. Desktop keeps the 320 file. Everything stays lazy, and the source and the img both carry 16:10 width/height. At 390x844@3, 7 of the 8 plates a walk to the bottom loads draw a card-size file (plates 340 CSS px wide). The phone total went from 744 KB on load to 1219 KB after the walk, and the folded years' images do not load. Not done: the other 61 row thumbs came from remote paper figures with no larger copy in the repo, and the generator that cut them is not committed. They stay 320 until a media pass makes larger cuts.
- **L6 done** (79fb37d): the people.json note no longer mentions the verify tag, and the CommunityJoin docstring now says landing section 08 (06 on /about). `caption_verify` and `LeaderboardReplay.tsx` are untouched, as asked.
- **L3, L5 skipped** as briefed (L3 is Cedric's call; L5 cannot trigger today).
